# config_appsettings submodule

This submodule manages the app settings configuration (`Microsoft.Web/sites/config`, name: `appsettings`) for an Azure App Service site.

It can be used independently to configure app settings on an existing site.
