# config_azurestorageaccounts submodule

This submodule manages the Azure Storage Accounts mount configuration (`Microsoft.Web/sites/config`, name: `azurestorageaccounts`) for an Azure App Service site.

It can be used independently to configure storage mounts on an existing site.
