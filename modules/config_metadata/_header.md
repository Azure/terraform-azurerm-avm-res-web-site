# config_metadata submodule

This submodule manages the metadata configuration (`Microsoft.Web/sites/config`, name: `metadata`) for an Azure App Service site.

It can be used independently to configure metadata (e.g. `CURRENT_STACK`) on an existing site.
