# config_slotconfignames submodule

This submodule manages the sticky slot configuration names (`Microsoft.Web/sites/config`, name: `slotConfigNames`) for an Azure App Service site.

It can be used independently to configure which app settings and connection strings are sticky (not swapped during slot swaps) on an existing site.
