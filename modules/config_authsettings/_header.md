# config_authsettings submodule

This submodule manages the authentication settings configuration (`Microsoft.Web/sites/config`, name: `authsettings`) for an Azure App Service site.

It can be used independently to configure authentication (v1) settings on an existing site.

> **Note:** This resource is deprecated. Use `config_authsettingsv2` for new deployments.
