# config_connectionstrings submodule

This submodule manages the connection strings configuration (`Microsoft.Web/sites/config`, name: `connectionstrings`) for an Azure App Service site.

It can be used independently to configure connection strings on an existing site.
