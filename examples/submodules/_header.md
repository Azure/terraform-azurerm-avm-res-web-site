# Submodules Example

This example demonstrates using the submodules independently to configure an existing App Service site. Each child resource type (app settings, connection strings, etc.) is deployed as a separate submodule call against the site's resource ID.
